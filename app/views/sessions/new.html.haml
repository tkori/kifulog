%h1 ログイン

.card.mx-auto.my-3.p-4.px-5
  = form_with scope: :session, local: true do |f|
    .form-group
      = f.text_field :email, class: 'form-control', id: 'session_email', placeholder: 'メールアドレス'
    .form-group
      = f.password_field :password, class: 'form-control', id: 'session_password', placeholder: 'パスワード'
    = f.submit 'ログインする', class: 'btn btn-primary mt-2'

  = form_with scope: :session, local: true do |f|
    = f.hidden_field :email, class: 'form-control', id: 'session_email', placeholder: 'メールアドレス', value: 'sample-user@gmail.com'
    = f.submit 'テストユーザーでログインする', class: 'btn btn-secondary mx-auto mt-4'